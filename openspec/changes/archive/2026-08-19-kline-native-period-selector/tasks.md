## 1. 标识符方案与后端映射

- [x] 1.1 确定月级时间级别的新标识符取值,确保与分钟级在大小写无关比较下不冲突,并记录到 design.md 的 Open Questions 解答处
- [x] 1.2 在 `backend/src/market_data/models.py` 补全 `_TIMEFRAME_STEP_MS`,覆盖原生全集中所有提供历史查询的级别(新增 `2h`、`3d`、月级;确认 `1w` 已有)
- [x] 1.3 在 `models.py` 补全 `_TIMEFRAME_GRANULARITY`,新增 `1s`、`2h`、`3d`、`1w`、月级到 Bitget granularity token 的映射
- [x] 1.4 修改 `_normalize_timeframe`,使归一化不再让月级与分钟级收敛为同一 key,同时保留 `1H`/`1h` 一类的大小写容错
- [x] 1.5 排查后端全链路的大小写归一化调用点,确认无残留处会把月级折叠为分钟级
- [x] 1.6 为映射表与归一化补单元测试:全集逐项可解析、月级与分钟级互不折叠、大小写容错仍生效、非原生级别抛出未支持异常
- [x] 1.7 确认现货与合约 REST granularity 名称差异(现货 `1week`/`1day` vs 合约 `1W`/`1D`)是否需要按品类分表,并据结论调整映射实现

## 2. 仅实时级别的后端约束

- [x] 2.1 在后端定义「仅实时级别」判定,将秒级标记为仅实时
- [x] 2.2 修改 `webapi.py` 的 `_seed_candles_from_rest`,对仅实时级别直接跳过 REST 补种
- [x] 2.3 确认 `/candles/recent` 对仅实时级别在缓存为空时返回空列表并建立实时订阅,不调用历史接口
- [x] 2.4 使 `ingestion` 的历史拉取、增量拉取、缺口校验、深度回灌均跳过仅实时级别
- [x] 2.5 确认 `realtime.py` 能正确拼接并订阅秒级 `candle1s` 通道,且推送按级别正确路由
- [x] 2.6 决定并调整 `config.py` 的 `timeframes` 默认值(定时抓取纳入哪些级别),确保不含仅实时级别
- [x] 2.7 补后端测试:仅实时级别不落盘、不回灌、不补种;秒级订阅通道拼接正确;级别间推送不串流

## 3. 存量数据兼容

- [x] 3.1 检查存量 parquet 目录中是否存在受月级标识符重命名影响的序列
- [x] 3.2 若存在受影响序列,实现目录迁移或读取期兼容,并验证迁移后 `/candles` 读取正常

## 4. 前端数据层

- [x] 4.1 扩展 `frontend/src/api/datafeed.ts` 的 `periodFromTimeframe`,支持秒/分/时/天/周/月标识符解析,移除仅认 `m|h|d` 的限制
- [x] 4.2 扩展 `periodToTimeframe` 的 `switch`,新增 `second`/`week`/`month` 分支,并保证月级不被压成分钟级
- [x] 4.3 使未知时间级别标识符可被调用方察觉,不再静默回落为 5 分钟
- [x] 4.4 在 `BitgetDatafeed.getHistoryKLineData` 中对仅实时级别短路返回空数组,不发起后端历史请求
- [x] 4.5 使 `prefetchDeeper` 与 backfill 逻辑跳过仅实时级别
- [x] 4.6 更新 `frontend/src/types/trading.ts` 的 `Timeframe` 联合类型,对齐原生全集
- [x] 4.7 补前端测试:全集往返一致、月级与分钟级不混淆、周月级不回落分钟级、未知标识符可察觉、仅实时级别不请求历史

## 5. vendor 秒级支持

- [x] 5.1 在 `frontend/vendor/klinecharts-pro/src/ChartProComponent.tsx` 的 `adjustFromTo` 新增 `second` 分支,正确计算秒级历史区间
- [x] 5.2 在同文件 `formatDate` 新增 `second` 分支,时间轴与十字光标标签显示到秒
- [x] 5.3 验证秒级下图表时间轴标签、十字光标标签显示正确

## 6. 固定(pin)偏好存储

- [x] 6.1 新增 `frontend/src/lib/` 下的固定偏好 store,沿用 `alertsStore.ts` 的 localStorage + 订阅通知范式,使用 `raibro.*` 命名空间
- [x] 6.2 实现默认固定级别 `1m 15m 1H 6H 1D 1W 1M`,在无存储记录时生效
- [x] 6.3 实现存储损坏/不可用时回退到默认固定级别,不抛异常
- [x] 6.4 支持固定级别为空的持久化(区分「空列表」与「无记录」两种状态)
- [x] 6.5 补 store 测试:默认值、增删固定、空列表持久化、内容损坏回退、存储不可用仍可用

## 7. 周期条改造

- [x] 7.1 扩展 `frontend/src/components/chart/KLineChartProView.tsx` 的 `NATIVE_PERIODS` 至原生全集 13 项,时间跨度与语义一致
- [x] 7.2 将 vendor `period-bar/index.tsx` 的周期平铺渲染改为按已固定级别渲染,并新增扩展按钮
- [x] 7.3 实现扩展弹窗,复用 vendor `component/modal`,按时间单位分组展示原生全集
- [x] 7.4 弹窗内实现级别切换与固定状态标示(含仅实时级别的可识别标示)
- [x] 7.5 实现弹窗内添加/取消固定,变更立即反映到常驻栏且不改变当前生效级别、不触发数据重载
- [x] 7.6 处理固定为空的布局:常驻栏无级别按钮时扩展按钮仍可见可点,`period-bar/index.less` 中依赖相邻元素的间距/分隔选择器不塌陷
- [x] 7.7 补充周期栏与弹窗的样式(扩展按钮、弹窗分组、固定标记、选中态),适配深浅主题
- [x] 7.8 确认周期条改造后中文 i18n 显示正常

## 8. 验证

- [x] 8.1 逐一验证原生全集中每个级别可正常切换并加载数据(秒级除外,验证其实时累积)
- [x] 8.2 验证月级与分钟级并存无混淆:分别切换两者,数据与存储路径均不同
- [x] 8.3 验证秒级进出行为:切入从空开始逐秒累积、切出释放订阅、无失败请求噪音
- [x] 8.4 验证固定增删、全空、从空恢复,以及页面重载后偏好保持
- [x] 8.5 验证固定偏好跨品种切换保持一致
- [x] 8.6 实测秒级推送频次下的图表重绘表现,确认无明显卡顿
- [x] 8.7 运行前端测试与类型检查(`npm run test`、`npm run typecheck`)
- [x] 8.8 运行后端测试套件
